# Copy to .env.local and fill in. Server-side only — never exposed to the browser.

# LangGraph Platform deployment URL (from LangSmith)
LANGGRAPH_API_URL=https://your-deployment.us.langgraph.app
# LangSmith API key used to authenticate calls to the deployment (X-Api-Key)
LANGGRAPH_API_KEY=
